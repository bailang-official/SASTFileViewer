<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SASTFileViewer.MainWindow" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SASTFileViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SASTFileViewer"
    xmlns:models="using:SASTFileViewer.Models"
    mc:Ignorable="d"
    Title="SASTFileViewer">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <!--将窗口分为两块，Auto高度由内容决定；*高度占据剩余全部空间-->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="12" Spacing="8">
            <!--横向排开-->
                <Button x:Name="BackButton" Click="BackButton_Click" ToolTipService.ToolTip="返回上一级">
                <SymbolIcon Symbol="Back"/>
                <!--调用GoBack方法-->
            </Button>

            <AutoSuggestBox x:Name="SearchBox" 
                    PlaceholderText="搜索文件..." 
                    QueryIcon="Find" 
                    Width="250"
                    TextChanged="SearchBox_TextChanged" />
            
            <BreadcrumbBar x:Name="PathBreadcrumbBar"
               ItemsSource="{x:Bind ViewModel.Breadcrumbs}"
               ItemClicked="PathBreadcrumbBar_ItemClicked"
               VerticalAlignment="Center"
               FontSize="14"/>
        </StackPanel>

        <ListView
            x:Name="FileListView"
            Grid.Row="1" 
            ItemsSource="{x:Bind ViewModel.Files}"
            DoubleTapped="FileListView_DoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:FileItem">
                    <Grid Padding="12,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <SymbolIcon Symbol="{Binding FileIcon}" 
                                        Foreground="{Binding IsFolder, Converter={StaticResource MyColorConverter}}"
                                        Margin="0,0,12,0" />
                            <!--StacticResource——App.xaml；Maigin左上右下，避免图标和文件名间距过近-->
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                            <!--根据FileItem对象的Name属性获得文件名并显示；Center垂直居中对齐-->
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="{Binding FileSize}" VerticalAlignment="Center" Foreground="Gray" FontSize="12"/>
                        <TextBlock Grid.Column="2" Text="{Binding LastModified}" VerticalAlignment="Center" Foreground="Gray" FontSize="12"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
